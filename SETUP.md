# دليل الإعداد السريع - Quick Setup Guide

## خطوة بخطوة لتشغيل البوت

### 1️⃣ تحميل الملفات
```bash
git clone https://github.com/osama182588-prog/bot-secret-police.git
cd bot-secret-police
npm install
```

### 2️⃣ إنشاء البوت في Discord

1. اذهب إلى: https://discord.com/developers/applications
2. اضغط "New Application"
3. اكتب اسم البوت (مثال: "شرطة الإجازات")
4. اذهب إلى قسم "Bot"
5. اضغط "Add Bot"
6. فعّل الخيارات التالية:
   - ✅ Presence Intent
   - ✅ Server Members Intent
   - ✅ Message Content Intent

### 3️⃣ نسخ التوكن والمعرفات

#### Bot Token:
- من صفحة "Bot"
- اضغط "Reset Token"
- انسخ التوكن (احفظه في مكان آمن!)

#### Client ID:
- من صفحة "General Information"
- انسخ "Application ID"

### 4️⃣ إضافة البوت للسيرفر

1. من صفحة "OAuth2" > "URL Generator"
2. اختر:
   - ✅ bot
   - ✅ applications.commands
3. في Bot Permissions اختر:
   - ✅ Manage Roles
   - ✅ Send Messages
   - ✅ Embed Links
   - ✅ Read Message History
   - ✅ Use Slash Commands
4. انسخ الرابط وافتحه في المتصفح
5. اختر السيرفر وأضف البوت

### 5️⃣ إنشاء الرومات والرتب

#### إنشاء 3 رومات:
1. **طلبات-الإجازات** (للأعضاء)
   - يمكن للجميع القراءة
   - لا يمكن لأحد الكتابة (إلا البوت)

2. **إدارة-الإجازات** (للإدارة فقط)
   - فقط الإدارة يمكنهم القراءة والكتابة

3. **سجل-الإجازات** (للوغ)
   - فقط الإدارة يمكنهم القراءة
   - فقط البوت يمكنه الكتابة

#### إنشاء رتبة إدارة:
- أنشئ رتبة باسم "إدارة الشرطة" أو أي اسم
- أعطها صلاحية "Manage Roles"

### 6️⃣ الحصول على المعرفات

فعّل وضع المطور:
- User Settings > Advanced > Developer Mode

#### Guild ID (معرف السيرفر):
- انقر يمين على اسم السيرفر > Copy ID

#### Channel IDs (معرفات الرومات):
- انقر يمين على كل روم > Copy ID

#### Role ID (معرف الرتبة):
- Server Settings > Roles
- انقر يمين على الرتبة > Copy ID

### 7️⃣ تعبئة config.json

افتح ملف `config.json` واستبدل القيم:

```json
{
  "token": "ضع التوكن هنا",
  "clientId": "ضع الـ Client ID هنا",
  "guildId": "ضع معرف السيرفر هنا",
  "channels": {
    "leaveRequests": "معرف روم طلبات الإجازات",
    "management": "معرف روم الإدارة",
    "logs": "معرف روم السجل"
  },
  "roles": {
    "management": "معرف رتبة الإدارة",
    "rejectedLeave": ""
  }
}
```

ملاحظة: `rejectedLeave` اختياري، سيتم إنشاء الرتبة تلقائياً

### 8️⃣ تشغيل البوت

```bash
npm start
```

انتظر رسالة:
```
✅ البوت جاهز! تم تسجيل الدخول باسم ...
```

### 9️⃣ إعداد النظام

في أي روم في السيرفر:
```
/إعداد_نظام_الإجازات
```

سيتم إنشاء الإمبيد الثابت في روم طلبات الإجازات.

### 🎉 تم! النظام جاهز

## اختبار النظام

1. اذهب إلى روم "طلبات-الإجازات"
2. اضغط زر "📝 تقديم إجازة"
3. املأ النموذج
4. تحقق من ظهور الطلب في روم "إدارة-الإجازات"
5. اضغط "قبول" أو "رفض"
6. تحقق من الرسالة الخاصة
7. تحقق من السجل في روم "سجل-الإجازات"

## مشاكل شائعة

### البوت لا يرد:
- ✅ تأكد من تفعيل جميع الـ Intents
- ✅ تأكد من صحة التوكن
- ✅ تأكد من تشغيل البوت (npm start)

### الأوامر لا تظهر:
- ⏳ انتظر 5-10 دقائق
- 🔄 أعد تشغيل Discord
- ✅ تأكد من صحة clientId و guildId

### البوت لا يستطيع إنشاء الرتب:
- 📊 رتبة البوت يجب أن تكون أعلى
- ✅ تأكد من صلاحية "Manage Roles"

### لا تصل رسائل خاصة:
- 🔓 تأكد من فتح الرسائل الخاصة
- ✅ تأكد من وجود البوت في السيرفر

## دعم

إذا احتجت مساعدة:
1. تحقق من Console للأخطاء
2. راجع ملف README.md
3. افتح Issue في GitHub

---

**بالتوفيق! 🚀**
