# 🔧 دليل حل المشاكل - Troubleshooting Guide

## مشاكل شائعة وحلولها

### 1. البوت لا يشتغل / البوت offline

#### الأعراض:
- البوت يظهر offline في Discord
- لا يستجيب للأوامر
- رسالة خطأ عند `npm start`

#### الحلول:

**أ) تحقق من التوكن:**
```bash
# افتح config.json وتأكد من صحة التوكن
# التوكن يجب أن يكون بهذا الشكل:
# [REDACTED]
```

**ب) تحقق من الـ Intents:**
1. اذهب إلى Discord Developer Portal
2. قسم "Bot"
3. Privileged Gateway Intents - فعّل:
   - ✅ Presence Intent
   - ✅ Server Members Intent
   - ✅ Message Content Intent

**ج) تحقق من تثبيت المكتبات:**
```bash
npm install
# إذا استمرت المشكلة:
rm -rf node_modules package-lock.json
npm install
```

**د) تحقق من إصدار Node.js:**
```bash
node --version
# يجب أن يكون 16.9.0 أو أحدث
```

---

### 2. الأوامر لا تظهر / Slash commands not showing

#### الأعراض:
- الأوامر لا تظهر عند كتابة `/`
- خطأ "Unknown Command"

#### الحلول:

**أ) تأكد من صحة المعرفات:**
```json
// في config.json
"clientId": "صحيح؟",
"guildId": "صحيح؟"
```

**ب) انتظر قليلاً:**
- الأوامر تحتاج 5-10 دقائق للظهور
- جرب:
  - إعادة تشغيل Discord
  - تسجيل الخروج والدخول

**ج) أعد تسجيل الأوامر:**
```bash
# أعد تشغيل البوت
npm start
# شاهد الكونسول:
# ✅ تم تسجيل X أمر بنجاح
```

**د) تحقق من الصلاحيات:**
- البوت يجب أن يكون لديه صلاحية `applications.commands`

---

### 3. البوت لا يستطيع إنشاء/إعطاء الرتب

#### الأعراض:
- خطأ "Missing Permissions"
- الرتب لا تُعطى للأعضاء
- خطأ عند قبول الطلب

#### الحلول:

**أ) تحقق من موقع رتبة البوت:**
1. إعدادات السيرفر > Roles
2. رتبة البوت يجب أن تكون **أعلى** من:
   - الرتب التي سينشئها
   - رتبة "لم تُقبل إجازته"
   - رتب الإجازات

**ب) تحقق من صلاحية Manage Roles:**
- رتبة البوت > Permissions
- ✅ Manage Roles

**ج) تحقق من الـ role hierarchy:**
```
[أعلى]
    └─ رتبة البوت
        └─ رتبة الإدارة
            └─ رتب الإجازات
                └─ رتب الأعضاء
[أقل]
```

---

### 4. الرسائل الخاصة لا تصل للأعضاء

#### الأعراض:
- الأعضاء لا يستقبلون إشعارات القبول/الرفض
- خطأ في الكونسول: "Cannot send messages to this user"

#### الحلول:

**أ) تحقق من إعدادات العضو:**
- العضو يجب أن يفتح DMs من أعضاء السيرفر:
  - Privacy & Safety > Allow direct messages from server members

**ب) تحقق من أن البوت في نفس السيرفر:**
- البوت والعضو يجب أن يكونا في نفس السيرفر

**ج) هذا طبيعي في بعض الحالات:**
- إذا كان العضو أغلق DMs، البوت لا يستطيع الإرسال
- سيتم تسجيل خطأ في الكونسول فقط
- الطلب سيستمر بشكل طبيعي

---

### 5. خطأ "لم يتم العثور على روم"

#### الأعراض:
```
❌ خطأ في النظام: لم يتم العثور على روم الإدارة (ID: ...)
```

#### الحلول:

**أ) تحقق من معرفات الرومات في config.json:**
```json
"channels": {
  "leaveRequests": "هل المعرف صحيح؟",
  "management": "هل المعرف صحيح؟",
  "logs": "هل المعرف صحيح؟"
}
```

**ب) تحقق من أن الرومات موجودة:**
- الرومات يجب أن تكون في نفس السيرفر المحدد في `guildId`

**ج) تحقق من صلاحيات البوت:**
- البوت يجب أن يرى الرومات
- ✅ View Channels في كل روم

---

### 6. قاعدة البيانات فارغة / البيانات اختفت

#### الأعراض:
- `/طلباتي` يقول "لم تقم بتقديم أي طلبات"
- `database.json` فارغ أو محذوف

#### الحلول:

**أ) تحقق من وجود database.json:**
```bash
ls -la database.json
```

**ب) استرجع من النسخة الاحتياطية:**
```bash
# إذا كنت تأخذ نسخ احتياطية:
cp database.backup.YYYYMMDD.json database.json
```

**ج) أعد تشغيل البوت:**
- سيتم إنشاء `database.json` جديد تلقائياً

---

### 7. الإمبيد الثابت اختفى

#### الأعراض:
- الإمبيد الثابت لم يعد موجوداً في روم طلبات الإجازات
- لا يوجد زر "تقديم إجازة"

#### الحلول:

**أ) أعد إنشاء الإمبيد:**
```
/إعداد_نظام_الإجازات
```

**ب) تحقق من الصلاحيات:**
- البوت يجب أن يستطيع:
  - ✅ Send Messages
  - ✅ Embed Links
  - في روم طلبات الإجازات

---

### 8. النموذج (Modal) لا يفتح

#### الأعراض:
- لا شيء يحدث عند الضغط على "تقديم إجازة"
- خطأ في الكونسول

#### الحلول:

**أ) تحقق من إصدار Discord.js:**
```bash
npm list discord.js
# يجب أن يكون 14.x.x
```

**ب) أعد تشغيل البوت:**
```bash
# Ctrl+C لإيقاف البوت
npm start
```

**ج) تحقق من الكونسول للأخطاء:**
- شاهد الأخطاء وشاركها للحصول على مساعدة

---

### 9. خطأ "Unknown Interaction"

#### الأعراض:
```
The application did not respond
```

#### الحلول:

**أ) تأكد من استمرارية عمل البوت:**
- البوت يجب أن يكون مشتغلاً طوال الوقت
- استخدم PM2 أو screen للتشغيل المستمر

**ب) لا توقف البوت أثناء التفاعل:**
- إذا أوقفت البوت بعد ضغط الزر، ستحصل على هذا الخطأ

**ج) تحقق من الـ deferReply:**
- الكود يحتوي على `deferReply` صحيح

---

### 10. أخطاء JSON في config.json

#### الأعراض:
```
SyntaxError: Unexpected token } in JSON at position ...
```

#### الحلول:

**أ) تحقق من صحة JSON:**
- استخدم: https://jsonlint.com/
- الصق محتوى config.json وتحقق من الأخطاء

**ب) تحقق من الفواصل:**
```json
{
  "key1": "value1",  // ✅ فاصلة
  "key2": "value2",  // ✅ فاصلة
  "key3": "value3"   // ❌ لا فاصلة في الأخير
}
```

**ج) تحقق من الأقواس:**
- كل `{` يجب أن يُغلق بـ `}`
- كل `[` يجب أن يُغلق بـ `]`

---

## 🆘 الحصول على مساعدة

### إذا لم تحل المشكلة:

1. **اجمع المعلومات:**
   - نص الخطأ من الكونسول
   - إصدار Node.js: `node --version`
   - إصدار Discord.js: `npm list discord.js`
   - خطوات إعادة المشكلة

2. **افحص الملفات:**
   ```bash
   # تحقق من صحة الأكواد
   node --check index.js
   node --check database.js
   node --check events/interactionCreate.js
   ```

3. **راجع السجلات:**
   - شاهد الكونسول للأخطاء
   - ابحث عن رسائل بـ ❌

4. **افتح Issue على GitHub:**
   - ضع كل المعلومات
   - ضع نص الخطأ
   - اشرح ما جربت

---

## 📊 تشخيص سريع

```bash
# اختبار سريع للنظام
node --version  # يجب أن يكون >= 16.9.0
npm list discord.js  # يجب أن يكون 14.x.x
node --check index.js  # يجب ألا يظهر أخطاء
ls -la config.json database.json  # يجب أن يكونا موجودين
cat config.json | jq .  # يتحقق من صحة JSON (إذا كان jq مثبت)
```

---

**آخر تحديث:** نوفمبر 2024
**الإصدار:** 1.0
