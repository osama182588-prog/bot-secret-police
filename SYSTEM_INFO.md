# 📊 معلومات النظام - System Information

## نظرة عامة على الملفات

### الملفات الرئيسية

#### `index.js` - ملف البوت الرئيسي
- تهيئة البوت
- تحميل الأوامر والأحداث
- تسجيل الأوامر مع Discord API
- معالجة الأخطاء العامة

#### `database.js` - مدير قاعدة البيانات
- حفظ واسترجاع طلبات الإجازات
- إدارة حالة الطلبات
- حفظ الملاحظات الإدارية
- نظام JSON بسيط وفعال

#### `utils.js` - وظائف مساعدة
- التحقق من صحة البيانات
- تنسيق التواريخ
- معالجة الأخطاء
- وظائف عامة مساعدة

#### `config.json` - ملف الإعدادات
- معلومات البوت والسيرفر
- معرفات الرومات والرتب
- يجب تعديله قبل التشغيل

### مجلد `commands/` - الأوامر

#### `setup.js` - أمر الإعداد
- إنشاء الإمبيد الثابت
- إعداد زر تقديم الطلبات
- للمسؤولين فقط

#### `my-requests.js` - أمر الطلبات الشخصية
- عرض طلبات العضو
- معلومات تفصيلية لكل طلب
- متاح للجميع

#### `leave-logs.js` - أمر السجل الكامل
- عرض جميع الطلبات
- إحصائيات شاملة
- للإدارة فقط

#### `add-note.js` - أمر إضافة ملاحظة
- إضافة ملاحظات إدارية
- تسجيل في اللوغ
- للإدارة فقط

### مجلد `events/` - معالجات الأحداث

#### `interactionCreate.js` - معالج التفاعلات
- معالجة ضغطات الأزرار
- معالجة النماذج (Modals)
- نظام القبول والرفض
- إرسال الإشعارات

## تدفق العمل

### 1. تقديم طلب جديد
```
عضو يضغط "تقديم إجازة"
    ↓
يظهر نموذج Modal
    ↓
العضو يملأ البيانات
    ↓
التحقق من صحة البيانات
    ↓
حفظ في قاعدة البيانات
    ↓
إرسال إلى روم الإدارة
    ↓
تسجيل في اللوغ
    ↓
تأكيد للعضو
```

### 2. قبول الطلب
```
إدارة تضغط "قبول"
    ↓
إنشاء رتبة جديدة (أو استخدام موجودة)
    ↓
إعطاء الرتبة للعضو
    ↓
تحديث حالة الطلب في DB
    ↓
إرسال رسالة خاصة للعضو
    ↓
تحديث رسالة الإدارة
    ↓
تسجيل في اللوغ
```

### 3. رفض الطلب
```
إدارة تضغط "رفض"
    ↓
إعطاء رتبة "لم تُقبل إجازته"
    ↓
تحديث حالة الطلب في DB
    ↓
إرسال رسالة خاصة للعضو
    ↓
تحديث رسالة الإدارة
    ↓
تسجيل في اللوغ
```

## قاعدة البيانات

### هيكل البيانات

```json
{
  "requests": [
    {
      "id": "1234567890",
      "userId": "123456789012345678",
      "username": "user#1234",
      "reason": "سبب الإجازة",
      "duration": 7,
      "startDate": "2024-12-25",
      "endDate": "2025-01-01",
      "status": "قيد المراجعة",
      "submittedAt": "2024-12-20T10:30:00.000Z",
      "processedAt": null,
      "role": null,
      "adminNotes": [],
      "managementMessageId": "123456789012345678"
    }
  ],
  "embedMessages": {
    "channelId": "messageId"
  }
}
```

### حالات الطلب
- **قيد المراجعة** - طلب جديد لم تتم معالجته
- **مقبول** - تمت الموافقة على الطلب
- **مرفوض** - تم رفض الطلب

## الصلاحيات المطلوبة

### للبوت:
- ✅ View Channels
- ✅ Send Messages
- ✅ Embed Links
- ✅ Manage Roles
- ✅ Read Message History

### للأوامر الإدارية:
- ✅ Administrator (للإعداد)
- ✅ Manage Roles (للسجل والملاحظات)

## التخصيص

### تغيير الألوان
في الملفات، ابحث عن:
- `#0099ff` - أزرق (معلومات)
- `#00ff00` - أخضر (قبول)
- `#ff0000` - أحمر (رفض)
- `#ffa500` - برتقالي (انتظار)

### تغيير النصوص
جميع النصوص العربية موجودة في:
- أوامر `commands/*.js`
- معالج `events/interactionCreate.js`
- يمكن تعديلها حسب الحاجة

### إضافة حقول جديدة
1. أضف الحقل في النموذج (Modal)
2. احفظه في قاعدة البيانات
3. اعرضه في الإمبيدات

## الأمان

### ✅ ما تم تنفيذه:
- التحقق من صحة المدخلات
- معالجة الأخطاء الشاملة
- فحص الصلاحيات
- رسائل خاصة مشفرة

### ⚠️ تحذيرات:
- لا تشارك `config.json` أو `.env`
- لا تضع التوكن في GitHub
- راجع الصلاحيات بانتظام
- احتفظ بنسخة احتياطية من `database.json`

## الصيانة

### نسخ احتياطي يومي:
```bash
cp database.json database.backup.$(date +%Y%m%d).json
```

### تنظيف قاعدة البيانات:
يمكنك إزالة الطلبات القديمة يدوياً من `database.json`

### تحديث البوت:
```bash
git pull
npm install
npm start
```

## الأداء

### تحسينات مقترحة:
- استخدام MongoDB للسيرفرات الكبيرة
- إضافة cache للطلبات المتكررة
- تقسيم الأوامر لملفات أصغر
- إضافة rate limiting

### الحدود الحالية:
- يعرض أحدث 10 طلبات في `/طلباتي`
- يعرض أحدث 5 طلبات في `/سجل_الإجازات`
- لا حد لعدد الطلبات المحفوظة

## الدعم الفني

### سجلات الأخطاء:
- تحقق من Console للأخطاء
- الأخطاء تُسجل تلقائياً
- استخدم `console.log` للتتبع

### مشاكل شائعة:
راجع ملف `SETUP.md` للحلول

---

**آخر تحديث:** نوفمبر 2024
