# مثال على ملف config.json
# Example config.json file

هذا مثال موضح لملف `config.json` مع شرح لكل حقل:

```json
{
  "token": "YOUR_BOT_TOKEN_HERE",
  "clientId": "YOUR_CLIENT_ID_HERE",
  "guildId": "YOUR_GUILD_ID_HERE",
  "channels": {
    "leaveRequests": "CHANNEL_ID_1",
    "management": "CHANNEL_ID_2",
    "logs": "CHANNEL_ID_3"
  },
  "roles": {
    "management": "ROLE_ID",
    "rejectedLeave": ""
  }
}
```

## شرح الحقول

### `token` (مطلوب)
**توكن البوت من Discord Developer Portal**

- اذهب إلى: https://discord.com/developers/applications
- اختر تطبيقك
- قسم "Bot" > اضغط "Reset Token"
- انسخ التوكن والصقه هنا
- **مهم جداً**: لا تشارك هذا التوكن مع أحد!

مثال:
```json
"token": "[REDACTED]"
```

### `clientId` (مطلوب)
**معرف التطبيق (Application ID)**

- من نفس صفحة Developer Portal
- قسم "General Information"
- انسخ "Application ID"

مثال:
```json
"clientId": "1234567890123456789"
```

### `guildId` (مطلوب)
**معرف السيرفر (Server ID)**

- فعّل Developer Mode في Discord
- انقر يمين على اسم السيرفر
- اختر "Copy ID"

مثال:
```json
"guildId": "9876543210987654321"
```

### `channels.leaveRequests` (مطلوب)
**معرف روم طلبات الإجازات**

- الروم الذي سيظهر فيه الإمبيد الثابت
- يضغط الأعضاء على زر "تقديم إجازة" هنا
- يُفضل أن يكون read-only للأعضاء

مثال:
```json
"leaveRequests": "1111111111111111111"
```

### `channels.management` (مطلوب)
**معرف روم الإدارة**

- الروم الذي تظهر فيه طلبات الإجازات للمراجعة
- فقط الإدارة يجب أن تستطيع رؤيته
- هنا تظهر أزرار القبول والرفض

مثال:
```json
"management": "2222222222222222222"
```

### `channels.logs` (مطلوب)
**معرف روم السجلات**

- الروم الذي تُسجل فيه جميع الإجراءات
- يُفضل أن يكون read-only للإدارة فقط
- يُستخدم لتتبع النشاط

مثال:
```json
"logs": "3333333333333333333"
```

### `roles.management` (مطلوب)
**معرف رتبة الإدارة**

- الرتبة التي سيتم منشنها عند ورود طلب جديد
- يجب أن يكون لديها صلاحية "Manage Roles"
- انقر يمين على الرتبة > Copy ID

مثال:
```json
"management": "4444444444444444444"
```

### `roles.rejectedLeave` (اختياري)
**معرف رتبة الطلبات المرفوضة**

- إذا تركته فارغاً، البوت سينشئ رتبة جديدة
- إذا أردت استخدام رتبة موجودة، ضع معرفها هنا
- الرتبة تُعطى للأعضاء عند رفض طلباتهم

مثال (رتبة موجودة):
```json
"rejectedLeave": "5555555555555555555"
```

مثال (رتبة جديدة):
```json
"rejectedLeave": ""
```

## مثال كامل لملف config.json

```json
{
  "token": "[REDACTED]",
  "clientId": "1234567890123456789",
  "guildId": "9876543210987654321",
  "channels": {
    "leaveRequests": "1111111111111111111",
    "management": "2222222222222222222",
    "logs": "3333333333333333333"
  },
  "roles": {
    "management": "4444444444444444444",
    "rejectedLeave": ""
  }
}
```

## ✅ نصائح

1. **تأكد من صحة جميع المعرفات** - خطأ واحد سيمنع البوت من العمل
2. **لا تضع مسافات زائدة** - استخدم المعرفات كما هي
3. **تأكد من الفواصل** - كل سطر ينتهي بفاصلة ما عدا الأخير
4. **احفظ الملف بصيغة JSON** - تأكد من الامتداد `.json`
5. **لا تشارك التوكن** - هذا مفتاح بوتك!

## 🔐 الأمان

- ❌ لا ترفع `config.json` على GitHub
- ✅ استخدم `.env` للمشاريع الكبيرة
- ✅ احتفظ بنسخة احتياطية في مكان آمن
- ✅ غيّر التوكن إذا تم الكشف عنه بالخطأ

## 📝 ملاحظات إضافية

### كيف تحصل على معرفات القنوات:
1. فعّل Developer Mode في Discord
2. User Settings > Advanced > Developer Mode (ON)
3. انقر يمين على أي قناة > Copy ID

### كيف تحصل على معرفات الرتب:
1. اذهب لإعدادات السيرفر > Roles
2. انقر يمين على الرتبة > Copy ID

### إذا نسيت التوكن:
1. اذهب للـ Developer Portal
2. قسم "Bot" > اضغط "Reset Token"
3. احصل على توكن جديد (القديم سيتوقف)

---

**بالتوفيق! إذا احتجت مساعدة، راجع ملف SETUP.md**
